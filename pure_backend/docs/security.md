# Security Controls

Implemented controls:

- JWT auth with refresh session revocation
- Login lockout protection and failure thresholds
- HTTPS enforcement for API routes
- Sensitive field encryption helpers
- Attachment type/size/content consistency checks
- Organization + business-context ownership checks for attachment access
- Append-only operation logs with immutable hash chain
- Role-based masking for export previews and sensitive profile/attachment paths
